extends _layout

append main
  
  h2 Página
  p Posts descargados dinámicamente desde la Api de Wordpress.com. De esta forma separamos el frontend del backend, la presentación de los contenidos.

  section(id="post-container")
    div(id="posts")
    
  section(id="form")
    input(type="text", placeholder="introduce datos")
    input(type="button", id="myButton", value="Pulsar")
    input(type="submit")
